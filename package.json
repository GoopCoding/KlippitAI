{
  "name": "custom-ai",
  "version": "1.0.0",
  "description": "A fully custom AI assistant built with a Markov chain generator (no external API) using exactly 20 files.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node tests/testModel.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
